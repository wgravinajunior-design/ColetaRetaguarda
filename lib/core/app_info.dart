/// Identificação da versão em execução.
///
/// Mantenha em sincronia com o campo `version:` do `pubspec.yaml` ao publicar
/// uma release — é o número que aparece na tela de login e o que o verificador
/// de atualização compara com a última tag do GitHub.
library;

const String appVersao = '2.3.0';

/// Repositório de onde saem as atualizações (releases públicas).
const String repoAtualizacao = 'wgravinajunior-design/ColetaRetaguarda';
